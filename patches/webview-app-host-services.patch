--- a/webview/assets/app-initial~app-main~new-thread-panel-page~onboarding-page~login-route~appgen-library-page~~gpgl9un5-_t04Xpau.js
+++ b/webview/assets/app-initial~app-main~new-thread-panel-page~onboarding-page~login-route~appgen-library-page~~gpgl9un5-_t04Xpau.js
@@ -23096,7 +23096,18 @@
           (super(), (this.#e = e));
         }
       }),
-      (Eye = new Tye({ appActions: yx, appUpdates: pye, downloads: Cye })));
+      (Eye = new Tye({
+        ...window.__ELECTRON_SHIM__?.services,
+        appActions: yx,
+        appUpdates: pye,
+        downloads: Cye,
+        requestUserInputAutoResolution: {
+          ...window.__ELECTRON_SHIM__?.services?.requestUserInputAutoResolution,
+          recordConversationActivity: () => undefined,
+          setConversationPresented: () => undefined,
+          snooze: () => undefined,
+        },
+      })));
   });
 function Oye() {
   let { port1: e, port2: t } = new MessageChannel();
@@ -23110,6 +23121,11 @@
   );
 }
 async function kye() {
+  if (window.__ELECTRON_SHIM__ != null) {
+    ((Ox = Eye), (kx = Eye.services), kx.devboxService);
+    return;
+  }
+
   ((Ox = Oye()), (kx = await Ox.services), kx.devboxService);
 }
 var Ox,
