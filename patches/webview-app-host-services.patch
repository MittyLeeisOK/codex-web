--- a/webview/assets/app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~l16cgjx9-CcJF-AY4.js
+++ b/webview/assets/app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~l16cgjx9-CcJF-AY4.js
@@ -3887,10 +3887,17 @@
         }
       }),
       (Qn = new Zn({
+        ...window.__ELECTRON_SHIM__?.services,
         appActions: tn,
-        appUpdates: cn,
+        appUpdates: Object.assign(cn, {
+          setSparkleQueryParams: cn.setSparkleQueryParams ?? (() => undefined),
+        }),
         clientCoordination: Rn,
         downloads: Yn,
+        requestUserInputAutoResolution: {
+          ...window.__ELECTRON_SHIM__?.services?.requestUserInputAutoResolution,
+          recordConversationActivity: () => undefined,
+          setConversationPresented: () => undefined,
+          snooze: () => undefined,
+        },
       })));
   });
 function er() {
@@ -3905,6 +3912,11 @@
   );
 }
 async function tr() {
+  if (window.__ELECTRON_SHIM__ != null) {
+    ((nr = Qn), ($ = Qn.services), $.clientCoordination != null && Dn($.clientCoordination), $.devboxService);
+    return;
+  }
+
   ((nr = er()),
     ($ = await nr.services),
     $.clientCoordination != null && Dn($.clientCoordination),
