--- a/webview/assets/app-initial~artifact-tab-content.electron~app-main~appgen-settings-page~page~pull-request-c~cmwz7z7b-D8GlEgh9.js
+++ b/webview/assets/app-initial~artifact-tab-content.electron~app-main~appgen-settings-page~page~pull-request-c~cmwz7z7b-D8GlEgh9.js
@@ -13276,8 +13276,18 @@
     f = new EventTarget(),
     p = new Na(),
     m = s,
+    inputModeDisabled = !1,
+    getAttributes = () =>
+      inputModeDisabled
+        ? { spellcheck: `true` }
+        : { spellcheck: `true`, inputmode: `none` },
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        h.isDestroyed || h.setProps({ attributes: getAttributes }));
+    },
     h = new hn(null, {
-      attributes: { spellcheck: `true` },
+      attributes: getAttributes,
       state: Zt.create({
         schema: $,
         doc:
@@ -13408,6 +13418,17 @@
         (h.updateState(t),
           f.dispatchEvent(new CustomEvent(lt, { detail: e.docChanged })));
       },
+      handleDOMEvents: {
+        mousedown(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        touchstart(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        blur(e, t) {
+          return (setPointerInputMode(!1), !1);
+        },
+      },
       handlePaste(e, t) {
         if (t.defaultPrevented) return !0;
         let n = t.clipboardData,
