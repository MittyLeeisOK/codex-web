--- a/webview/index.html
+++ b/webview/index.html
@@ -8 +8,4 @@
-    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
+    <meta
+      name="viewport"
+      content="width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content"
+    />
@@ -131,6 +134,67 @@
         }
       }
     </style>
+    <style>
+      .main-surface {
+        --spacing-token-safe-header-left: 0px;
+      }
+
+      @media (pointer: coarse) {
+        html,
+        body,
+        #root {
+          height: var(--codex-web-visual-viewport-height, 100dvh);
+          min-height: var(--codex-web-visual-viewport-height, 100dvh);
+          -webkit-text-size-adjust: 100%;
+        }
+
+        textarea,
+        input,
+        [contenteditable="true"],
+        .ProseMirror {
+          font-size: 16px !important;
+        }
+      }
+    </style>
+    <script>
+      (() => {
+        const viewport = window.visualViewport;
+        if (!viewport) return;
+
+        const root = document.documentElement;
+        const syncVisualViewport = () => {
+          root.style.setProperty(
+            "--codex-web-visual-viewport-height",
+            `${Math.round(viewport.height)}px`,
+          );
+        };
+
+        const scheduleViewportSync = () => {
+          syncVisualViewport();
+          requestAnimationFrame(syncVisualViewport);
+        };
+
+        viewport.addEventListener("resize", scheduleViewportSync, {
+          passive: true,
+        });
+        viewport.addEventListener("scroll", scheduleViewportSync, {
+          passive: true,
+        });
+        window.addEventListener(
+          "orientationchange",
+          () => setTimeout(scheduleViewportSync, 250),
+          { passive: true },
+        );
+
+        if (document.readyState === "loading") {
+          document.addEventListener("DOMContentLoaded", scheduleViewportSync, {
+            once: true,
+          });
+        } else {
+          scheduleViewportSync();
+        }
+      })();
+    </script>
     <script type="module" crossorigin src="./assets/index-BtZb5ZU2.js"></script>
     <link
       rel="modulepreload"
